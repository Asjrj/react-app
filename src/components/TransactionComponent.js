import React from 'react'
import { Grid } from '@material-ui/core'

const OrderComponent = (props) => (
  <Grid container>
    <Grid item xs={12} style={{padding: 20}}>
    <h1> Transaction data here </h1>
    </Grid>  
  </Grid> 
)

export default OrderComponent